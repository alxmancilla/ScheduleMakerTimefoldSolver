# Database Configuration for School Schedule System
# Copy this file to database.properties and update with your credentials

# PostgreSQL JDBC URL
db.url=jdbc:postgresql://localhost:5432/school_schedule

# Database credentials
db.username=mancilla
db.password=

# Connection pool settings (optional)
db.pool.maxConnections=10
db.pool.minConnections=2

# Loaded automatically by `npm run dev` (vite). See .env.example for docs.
# In dev, /api is proxied to the backend by vite.config.js.
VITE_API_BASE_URL=/api
VITE_DEV_PROXY_TARGET=http://localhost:8080
